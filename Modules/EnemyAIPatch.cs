using HarmonyLib;

/*namespace LethalWarfare2.Modules
{
    [HarmonyPatch(typeof(EnemyAI))]
    internal class EnemyAIPatch
    {
        [HarmonyPatch("CheckLineOfSightForPlayer")]
        [HarmonyPrefix]
        private static bool CheckLineOfSightForPlayer(ref EnemyAI __instance, ref float ___width, ref int ___range, ref int ___proximityAwareness)
        {
            return true;
        }
    }
}*/
